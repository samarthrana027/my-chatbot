# my-chatbot

A React + Vite chatbot project.

## Project setup

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

## Notes

This repository contains a Vite-powered React application located in `src/`.
